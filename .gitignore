# Données personnelles — ne doivent jamais être publiées (dépôt public)
notified.json
cache_evenements.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# Fichiers de travail (relecture de traductions, etc.) — pas du code
*.xlsx
~$*
